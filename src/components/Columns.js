import React from 'react';

import PropTypes from 'prop-types';

function Columns({data}){
    return(
        <>
            <tr>
                
            </tr>
        </>
    );
}

export default Columns;